<div class="container">
  <h1>View post</h1>
  <p>Instructions here</p>

  <table>
    <tr>
      <td><%= @post.title %></td>
    </tr>
    <tr>
      <td>by <%= @post.author %></td>
    </tr>
    <tr>
      <td><%= @post.content %></td>
    </tr>
    <tr>
      <td>
        Tags: <% @post.tags.each do |tag| %>
        <%= tag.name %>&nbsp;
        <% end %>
      </td>
      </td>
    <tr>
      <td align="right">
        <a href="../../"><button>Main Page</button></a>
      </td>
    </tr>
  </table>
</div>
